#pragma once

#include <SDL2/SDL.h>
#include <string>

/* modes */
const int DEBUG_MODE = SDLK_F1;

/* movement */

const int TOGGLE_FLIGHT = SDLK_SPACE;
const int UP = SDLK_w;
const int LEFT = SDLK_a;
const int DOWN = SDLK_s;
const int RIGHT = SDLK_d;

/* commands */

const int RESPAWN = SDLK_RETURN;